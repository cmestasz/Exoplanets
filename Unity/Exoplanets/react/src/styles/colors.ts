const transitionColor = 'transition-colors duration-300 ease-out';
const DEFAULT_COLOR = `${transitionColor} text-primary hover:text-secondary`;
const INVERTED_COLOR = `${transitionColor} text-secondary hover:text-primary`;

export { DEFAULT_COLOR, INVERTED_COLOR };
